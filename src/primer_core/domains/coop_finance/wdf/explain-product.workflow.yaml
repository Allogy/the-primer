# Explain Product Workflow Definition — STUB (KG-W5 / RAG-1847)
#
# Minimal WDF structure only: name, entry, exit, nodes. It exists so the
# coop-finance pack registers a real, loadable skill and the orchestrator can
# resolve a workflow id for it.
#
# Real skill content (node prompts, retrieval steps, guardrails) is DS-W5 scope.

name: explain-product
entry: explain
exit: complete

nodes:
  explain: {}
  complete: {}
